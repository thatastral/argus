{
  "name": "argus",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=apps/web",
    "sync-abi": "cd contracts && forge build && for c in HabitManager PenaltyEngine ArgusFactory AccountabilityWallet MockUSDC; do forge inspect $c abi --json > ../apps/web/lib/abi/$c.json; done"
  }
}
